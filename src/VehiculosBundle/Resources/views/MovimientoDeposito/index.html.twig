    {% extends 'ThemeBundle:Default:layout_base.html.twig' %}

    {% block title %}
        MovimientoDeposito
    {% endblock title %}

    {% block content -%}


    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">Listado MovimientoDeposito</h3>


                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <table class="data-table table table-bordered table-striped">
                        <thead>
                            <tr>
                                    <th>Id</th>
                                    <th>Fechaingreso</th>
                                    <th>Fechaegreso</th>
                                    <th>Fila</th>
                                    <th>Posicion</th>
                                    <th>Observacion</th>
                                    <th>Actual</th>
                                    <th>Creado</th>
                                    <th>Actualizado</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for entity in entities %}
                            <tr>
                                        <td>
                                            <a href="{{ path('movimiento_deposito_show', { 'id': entity.id }) }}">{{ entity.id }}</a>
                                        </td>
                                        <td>{% if entity.fechaIngreso %}{{ entity.fechaIngreso|date('d/m/Y H:i:s') }}{% endif %}</td>
                                        <td>{% if entity.fechaEgreso %}{{ entity.fechaEgreso|date('d/m/Y H:i:s') }}{% endif %}</td>
                                        <td>{{ entity.fila }}</td>
                                        <td>{{ entity.posicion }}</td>
                                        <td>{{ entity.observacion }}</td>
                                        <td>{{ entity.actual }}</td>
                                        <td>{% if entity.creado %}{{ entity.creado|date('d/m/Y H:i:s') }}{% endif %}</td>
                                        <td>{% if entity.actualizado %}{{ entity.actualizado|date('d/m/Y H:i:s') }}{% endif %}</td>
                                        <td>

                        <a class="btn btn-default" href="{{ path('movimiento_deposito_show', { 'id': entity.id }) }}"><i class="fa fa-eye"></i></a>

                        <a class="btn btn-default" href="{{ path('movimiento_deposito_edit', { 'id': entity.id }) }}"><i class="fa fa-edit"></i></a>

                                        </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
                <div class="box-footer">
                    
                        <a class="btn btn-primary" href="{{ path('movimiento_deposito_new') }}">
                            Nuevo
                        </a>

                                    </div>
            </div>
        </div>
    </div>
    {{ knp_pagination_render(entities) }}
    {% endblock %}
