{% extends 'ThemeBundle:Default:layout_base.html.twig' %}

{% block content_title %}
    Formulario de Edicion
{% endblock %}

{% block content -%}
<div class="row">
    <div class="col-md-12">
        <div class="box box-primary">
            <div class="box-header with-border">
                <h3 class="box-title">TipoVentaEspecial</h3>
            </div>

    {{ form(edit_form) }}

        <div class="box-footer">
<a class="btn btn-default" href="{{ path('tipo_venta_especial') }}">
    Volver
</a>

</div>
</div>
</div>
</div>
    {{ form(delete_form) }}

{% endblock %}
