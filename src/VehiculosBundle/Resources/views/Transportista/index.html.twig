    {% extends 'ThemeBundle:Default:layout_base.html.twig' %}

    {% block title %}
        Transportista
    {% endblock title %}

    {% block content -%}


    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">Listado Transportista</h3>


                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <table class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                    <th>Id</th>
                                    <th>Cuit</th>
                                    <th>Apellido</th>
                                    <th>Nombre</th>
                                    <th>Telefono</th>
                                    <th>Domicilio</th>
                                    <th>Creado</th>
                                    <th>Actualizado</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for entity in entities %}
                            <tr>
                                        <td>
                                            <a href="{{ path('transportistas_show', { 'id': entity.id }) }}">{{ entity.id }}</a>
                                        </td>
                                        <td>{{ entity.cuit }}</td>
                                        <td>{{ entity.apellido }}</td>
                                        <td>{{ entity.nombre }}</td>
                                        <td>{{ entity.telefono }}</td>
                                        <td>{{ entity.domicilio }}</td>
                                        <td>{% if entity.creado %}{{ entity.creado|date('Y-m-d H:i:s') }}{% endif %}</td>
                                        <td>{% if entity.actualizado %}{{ entity.actualizado|date('Y-m-d H:i:s') }}{% endif %}</td>
                                        <td>

                        <a class="btn btn-default" href="{{ path('transportistas_show', { 'id': entity.id }) }}"><i class="fa fa-eye"></i></a>

                        <a class="btn btn-default" href="{{ path('transportistas_edit', { 'id': entity.id }) }}"><i class="fa fa-edit"></i></a>

                                        </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
                <div class="box-footer">
                    
                        <a class="btn btn-primary" href="{{ path('transportistas_new') }}">
                            Nuevo
                        </a>

                                    </div>
            </div>
        </div>
    </div>
    {{ knp_pagination_render(entities) }}
    {% endblock %}
