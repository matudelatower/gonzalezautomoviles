    {% extends 'ThemeBundle:Default:layout_base.html.twig' %}

    {% block content -%}


    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">Listado Persona</h3>

                    <div class="box-tools">
                        <div class="input-group" style="width: 150px;">
                            <input type="text" name="table_search" class="form-control input-sm pull-right"
                                   placeholder="Buscar">

                            <div class="input-group-btn">
                                <button class="btn btn-sm btn-default"><i class="fa fa-search"></i></button>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- /.box-header -->
                <div class="box-body table-responsive no-padding">
                    <table class="table table-hover">

                        <tr>
                                <th>Id</th>
                                <th>Nombre</th>
                                <th>Apellido</th>
                                <th>Numerodocumento</th>
                                <th>Telefono</th>
                                <th>Celular</th>
                                <th>Mail</th>
                                <th>Web</th>
                                <th>Fechanacimiento</th>
                                <th>Calle</th>
                                <th>Numerocalle</th>
                                <th>Barrio</th>
                                <th>Creado</th>
                                <th>Actualizado</th>
                            <th>Acciones</th>
                        </tr>

                        {% for entity in entities %}
                        <tr>
                                    <td>
                                        <a href="{{ path('personas_show', { 'id': entity.id }) }}">{{ entity.id }}</a>
                                    </td>
                                    <td>{{ entity.nombre }}</td>
                                    <td>{{ entity.apellido }}</td>
                                    <td>{{ entity.numeroDocumento }}</td>
                                    <td>{{ entity.telefono }}</td>
                                    <td>{{ entity.celular }}</td>
                                    <td>{{ entity.mail }}</td>
                                    <td>{{ entity.web }}</td>
                                    <td>{% if entity.fechaNacimiento %}{{ entity.fechaNacimiento|date('Y-m-d H:i:s') }}{% endif %}</td>
                                    <td>{{ entity.calle }}</td>
                                    <td>{{ entity.numeroCalle }}</td>
                                    <td>{{ entity.barrio }}</td>
                                    <td>{% if entity.creado %}{{ entity.creado|date('Y-m-d H:i:s') }}{% endif %}</td>
                                    <td>{% if entity.actualizado %}{{ entity.actualizado|date('Y-m-d H:i:s') }}{% endif %}</td>
                                    <td>

                        <a class="btn btn-default" href="{{ path('personas_show', { 'id': entity.id }) }}"><i class="fa fa-eye"></i></a>

                        <a class="btn btn-default" href="{{ path('personas_edit', { 'id': entity.id }) }}"><i class="fa fa-edit"></i></a>

                                    </td>
                        </tr>
                        {% endfor %}

                    </table>
                </div>
                <div class="box-footer">
                    
                        <a class="btn btn-primary" href="{{ path('personas_new') }}">
                            Nuevo
                        </a>

                                    </div>
            </div>
        </div>
    </div>
    {{ knp_pagination_render(entities) }}
    {% endblock %}
