app_item_aplicativo:
    resource: "@AppBundle/Resources/config/routing/itemaplicativo.yml"
    prefix:   /item_aplicativo

app_aplicativo:
    resource: "@AppBundle/Resources/config/routing/aplicativo.yml"
    prefix:   /aplicativo

app_homepage:
    path:     /
    defaults: { _controller: AppBundle:Default:index }

app_pantalla_vacia:
    path: /app_pantalla_vacia
    defaults: { _controller: "AppBundle:Default:pantallaVacia" }

app_bloquear_pantalla:
    path: /app_bloquear_pantalla
    defaults: { _controller: "AppBundle:Default:bloquearPantalla" }

app_desbloquear_pantalla:
    path: /app_desbloquear_pantalla
    defaults: { _controller: "AppBundle:Default:desbloquearPantalla" }
